#include "main.h"
#include <stdio.h>

/**
*print_line - function that prints a line
*
*Return: void
*/
void print_line(int n)
{
	int n;

	for (n = 0 ; n <= 15 ; n++)
	{
		if (i < 16)
			_putchar('_');
		else if (i <= 0)
			_putchar('\\n');
	}
	_putchar('\n')
}
